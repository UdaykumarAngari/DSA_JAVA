class Solution {
    public boolean uniformArray(int[] nums1) {
        return true;


        // int oddCount = 0 ;
        // int n = nums1.length;

        // for(int num : nums1){
        //     if(Math.abs(num)%2 != 0){
        //         oddCount++;
        //     }
        // }

        // boolean canBeAllEven = (oddCount== 0 || oddCount >=2);

        // Boolean canBeAllOdd = (oddCount >0);

        // return canBeAllEven || canBeAllOdd;
    }
}